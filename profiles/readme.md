# profiles

Installation profiles for the dev-ai-utilities installer.

Installation profiles for the dev-ai-utilities installer. Each profile declares which plugins and cursor rules to install for a target project. Schema in profile-schema.json.
