# plugins

Domain-specific plugin bundles for different project types.

Each subdirectory is a self-contained Claude Code plugin installed alongside the core-superpowers plugin based on the selected profile.
